{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "profilePhoto": {
      "type": "string",
      "description": "URL of the user's profile photo"
    }
  },
  "required": [
    "role"
  ]
}